const router = require('express').Router();

// Clothing Item routes:
router.get('/', () => { });
router.post('/', () => { });
router.delete('/:itemId', () => { });

// Exports:
module.exports = router;