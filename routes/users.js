// Imports:
const router = require('express').Router();

// User routes:
router.get('/', () => { });
router.get('/:userId', () => { });
router.post('/', () => { });

// Exports:
module.exports = router;